NotchQuota 0.1.5：加入开机自动启动。

右键刘海区域，勾选“开机自动启动”，即可在下次登录 macOS 后自动运行；取消勾选即可关闭。采用系统原生登录项，实时读取启用状态；需要系统允许时提供设置入口，不增加常驻辅助进程。

请先将应用放入 Applications。Apple Silicon / macOS 13+，Developer ID 签名并经 Apple 公证。保留紧凑常驻、默认应用优先级与原有额度刷新频率。
