轻量原生 macOS 刘海额度应用，适用于 Apple Silicon / macOS 13+。

- 原生图标切换 Codex、Claude、Antigravity。
- 绿、黄、红显示剩余额度；15 秒无操作自动隐藏。
- 已保存的登录可独立查询额度，原应用无需保持运行。

下载 DMG，拖入 Applications。首次使用需要原应用已有有效登录，并可能需要允许读取相应钥匙串。

安装包使用 Developer ID 签名并经过 Apple 公证。首版尚未覆盖所有账户类型和 macOS 版本；接口变化或登录失效时可能需重新登录。详情与反馈见 README。
