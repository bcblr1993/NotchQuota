NotchQuota 0.1.15：改进 Antigravity 手动添加实例。

- 明确标注两步流程：先选择 Antigravity 应用，再选择对应的 OAuth 认证文件。
- 认证文件选择默认打开本机 .gemini 目录，并显示隐藏文件。
- 显式允许无扩展名的 standalone-oauth-token 文件，无需改名为 .json；添加前仍校验登录格式，不修改认证文件。

验证：39 项单元测试与 59 项原有 UI 回归通过；已在本机确认无扩展名文件显示在第二步列表。因电脑操作中断，最后的选中与提交添加动作未完成端到端验证。

本机测试系统为 macOS 26.5.2；macOS 27 仍需真机验收。
