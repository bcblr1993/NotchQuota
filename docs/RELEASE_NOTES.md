NotchQuota 0.1.12：菜单栏模式与单账号详情。

在“设置与更新”切换“灵动岛模式 / 菜单栏模式”。菜单栏显示单色图标与额度，点击打开当前账号的各周期、模型额度和重置时间。点击面板头像循环切换已勾选账号，只有一个账号时保持不变。详情宽度 320 点，高度随额度项数量调整；保留每分钟自动轮换和轻量切换过渡。

两种模式共用数据，保留账号选择、头像、临时隐藏和恢复入口；关闭面板停止界面计时。macOS 27 未设置显示模式时默认菜单栏，已手动选择的模式保持不变。

已在 macOS 26.5.2 验证；macOS 27 系统溢出区域、全屏及外接屏仍需进一步真机验收。
