NotchQuota 0.1.1：更柔和的刘海过渡，更少的无用显示。

- 三枚图标统一为更小的 16 点视觉尺寸。隐藏后保留 1.5 点静态额度线，按 Codex → Claude → Antigravity 的安装顺序选择。
- 约 0.2 秒淡入淡出、展开收起和图标切换，支持减少动态效果及低电量模式。
- 只显示已安装的桌面应用；单应用不切换，全部未安装时保持隐藏。
- 图标内存复用，无循环动画，保留 15 秒自动隐藏。

适用于 Apple Silicon / macOS 13+，DMG 使用 Developer ID 签名并经 Apple 公证。拖入 Applications 替换旧版本即可。读取额度需要对应账号已有有效登录；首次可能请求钥匙串访问。尚未覆盖所有系统版本、机型和账户类型。
