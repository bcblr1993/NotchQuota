NotchQuota 0.1.3：紧凑额度常驻。

- 去掉额度线与呼吸效果，保持小图标和百分比常驻。
- 默认显示 Codex；未安装时依次选择 Claude、Antigravity，全部未安装时不显示。
- 点击图标临时切换，15 秒无操作收起详情并回到默认应用。
- 额度每 3 分钟更新，无空闲循环动画。

适用于 Apple Silicon / macOS 13+。安装包经 Developer ID 签名和 Apple 公证，拖入 Applications 替换旧版即可。读取额度需要原应用已有有效登录。
