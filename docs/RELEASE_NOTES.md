NotchQuota 0.1.14：修复侧边栏收缩黑影。

侧栏收起时立即清除黑色背景及未结束的背景动画，避免窗口缩小、圆角移除时出现黑块；保留透明图标过渡和鼠标移入立即展开。

支持原有四种外观，保留小幽灵半身倾斜探头、账号选择、多屏位置记忆及在线更新。

验证：38 项单元测试通过；81 条侧栏回归输出（含完成标记）与 59 项原有 UI 检查通过。新增收缩开始时背景和背景动画均已清除的检查。

本机测试系统为 macOS 26.5.2。此版本未完成逐帧流畅度测量，macOS 27 仍需真机验证。
