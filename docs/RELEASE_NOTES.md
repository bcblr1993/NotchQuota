NotchQuota 0.1.6：每分钟自动轮换额度。

按 Codex → Claude → Antigravity 轮换已安装应用，切换采用 0.38 秒缓入缓出淡化。查看详情、交互和休眠时暂停，15 秒收起后保留当前应用。手动点击可立即切换，重新开始一分钟计时。

自动轮换只展示缓存，额度仍每 3 分钟查询。单应用不启动轮换计时器；减少动态效果和低电量模式跳过动画。保留开机自动启动。

Apple Silicon / macOS 13+，Developer ID 签名并经 Apple 公证。
