# 0.1.4 验证记录

日期：2026-09-09；本机 macOS 26.5.2 / Apple Silicon。

- 7 个 XCTest 用例、16 项原生 UI smoke 通过，新增手动收起抑制立即重开、鼠标退出恢复悬停及休眠暂停刷新检查；紧凑/展开截图已检查。
- Developer ID 签名、Apple 公证 Accepted、应用和 DMG stapler 校验通过。本机安装后重新验证签名和版本。
- 新增外部 proc_pid_rusage 采样器，默认每 10 秒读一次内核计数，240 秒后退出。已对修改前后进行本机采样，数据仅本机保留；解释与限制见 PERFORMANCE.md。
- 减少冗余监听与布局更新不能据此宣称所有机器性能提升，短时采样也不替代长时趋势和续航验证。

# 0.1.3 验证记录

日期：2026-09-09；本机 macOS 26.5.2 / Apple Silicon。

- 7 个 XCTest 用例通过，覆盖安装优先级、单应用选择、图标尺寸、额度解析及空闲交互边界。
- 13 项原生 UI smoke 通过：15 秒后紧凑窗口常驻、默认 Codex、空闲计时器停止、后台数字更新不展开、交互/切换/收起、安装回退、空安装状态与仅一个可见窗口。
- 已检查常驻紧凑模式截图；移除额度线视图、呼吸动画及其专用电源/休眠监听。
- 查询周期仍为 180 秒，不是逐秒实时查询。空闲时无循环动画。

# 0.1.2 验证记录

日期：2026-09-09；本机 macOS 26.5.2 / Apple Silicon。

- 9 个 XCTest 用例通过，新增呼吸动画生命周期、低电量、减少动态效果及未知额度停止策略检查。
- 15 项原生 UI smoke 通过，包括系统合成层实际透明度随时间变化、待机启用动画、屏幕休眠状态停止动画、展开后移除动画。
- 检查高饱和度额度线截图；额度几何、图标归一化、安装优先级、15 秒收起与 3 分钟查询规则保持不变。
- 仅一个 opacity 动画，单程 2.4 秒、自动往返；无逐帧 Timer / DisplayLink，无动态阴影模糊。
- 电源/辅助功能/屏幕状态通过系统通知更新。低电量与减少动态效果分支通过调用策略参数测试，没有修改用户系统设置。
- 本机应用进程的短时采样不能代表 WindowServer/GPU 总开销或长期续航；仍需不同机型与系统的长期观察。

# 0.1.1 验证记录

日期：2026-09-09；本机 macOS 26.5.2 / Apple Silicon。

- 8 个 XCTest 用例通过，安装选择覆盖三种应用的全部 8 种组合。
- 原生 UI smoke 的 12 项检查通过：15 秒收起保留细线、Codex 默认优先级、热点唤出、隐藏中重新显示、展开中间帧、展开收起反向操作、顶部锚点、单应用不切换、无应用空状态、后台发现保持细线、未安装 Codex 时回退、新安装后唤出。
- 三个图标透明边距归一化后均为 16 点，单元测试覆盖；细线优先级不依赖安装检测顺序。
- 检查了紧凑、展开、仅 Claude、额度细线及动画中间帧截图；动态收起内容按圆角裁切。
- 安装组合通过测试注入模拟，未卸载或关闭用户的三个原应用。
- 过渡约 0.2 秒，没有循环动画；低电量/减少动态效果分支保留即时切换。未修改本机辅助功能或低电量设置进行实机验证。
- 额度查询维持 180 秒刷新、30 秒请求间隔下限；只查询已安装应用。后台刷新不展开主窗口、不重置 15 秒计时。

- 最终 DMG 经 Apple 公证 `Accepted`，DMG 和应用 stapler 校验通过；本机安装后签名与公证来源校验通过。
- 实际界面验证 Codex 切换 Claude、展开详情正常；此处使用真实登录数据，未公开截图。
- 加入细线前，本机启动、切换、展开后回到空闲的短时采样：CPU 0.0%，RSS 68,976 KiB（约 67 MiB）；这是单机快照，不代表峰值或长期保证。
- 动画及安装筛选阶段的提交 `2c0b853` 的 GitHub macOS CI 测试、arm64 构建和包校验全部成功：[CI 记录](https://github.com/bcblr1993/NotchQuota/actions/runs/34302775125)。

兼容性覆盖限制仍见下方首版记录。

# 0.1.0 验证记录

日期：2026-09-09；本机系统 macOS 26.5.2，Apple Silicon。

已完成：

- Apple Silicon 原生 release 构建，arm64 Mach-O；包内图标资源独立于源码/构建目录。
- Swift Package 的 3 个 XCTest 用例通过，覆盖额度解析、真实零值/未知字段、颜色阈值、15 秒计时、周期识别及服务地址白名单。
- 原生 `--ui-smoke`：实际窗口闲置 15 秒隐藏，退出再进入顶部热点后显示，全部通过；截图已检查。
- 本机刘海屏紧凑尺寸 279×32 点，其中摄像头留白约 185 点，新增两侧占用约 94 点。
- Codex、Claude 桌面会话和 Antigravity 钥匙串的真实查询均成功，未使用本机运行中应用的额度接口。三个原应用无需为请求保持运行。
- 使用正常系统代理；无浏览器后台、无第三方运行时依赖。
- Developer ID 签名、Apple notarization `Accepted`、DMG staple 和票据校验通过。
- 挂载分发 DMG 后，包内应用识别为 `Notarized Developer ID`；从挂载包执行三个提供商的查询均成功。
- 本机实际安装到 Applications。交互验证了图标从 Codex → Claude → Antigravity 切换。
- 安装包约 0.84 MiB，应用约 0.88 MiB。原型稳定空闲采样 CPU 为 0.0%、RSS 约 57 MiB；不同系统和网络阶段会波动。
- GitHub macOS CI 曾发现 Swift 并发闭包兼容性错误，已修复。修复提交 `dd11410` 的测试、arm64 构建和包校验全部通过：[CI 记录](https://github.com/bcblr1993/NotchQuota/actions/runs/34300030404)。

未完成的覆盖，不能宣称已通过：

- 所有 M 系列机型、所有受支持 macOS 版本与全部账户类型的独立实机矩阵。
- 为了不中断用户当前工作，没有退出本机正在使用的三个原应用；独立读取由无进程依赖的实现与直接远程请求验证。
- 当前验证机的 Gatekeeper 存在 `security disabled` 本机覆盖；来源、公证和签名均已识别，但这不等同于全新默认安全策略机器的首次安装验证。
- 长期运行、多次睡眠/唤醒、显示器热插拔以及失效登录的全场景验证。

公开 Issue 可追踪以上兼容性覆盖；不要上传未经脱敏的认证日志。
