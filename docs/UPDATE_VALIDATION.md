# 0.1.7 在线更新验证

2026-09-10，Apple Silicon 本机。

- `swift test`：9 项通过。
- Developer ID Release 构建、包布局及嵌套代码签名验证通过。
- `--ui-smoke`：24 项通过；可更新标记不改变 279 × 32 pt 紧凑窗口，测试模式不启动真实更新器。
- 安装包通过 Apple 公证及 stapler 验证。当前机器 Gatekeeper 存在既有策略覆盖，不能替代默认安全策略的干净机器验收。
- Sparkle 验证更新包和订阅签名成功，篡改订阅后验证失败。
- 版本文字从安装包 CFBundleShortVersionString 读取，更新提示从签名订阅读取。

更新调度由 Sparkle 管理，默认每日检查；手动检查不必等待一天。关闭自动检查仍可手动检查。下载和安装需要用户主动点击，不开启自动安装或系统信息上报。

本机端到端验证采用包含更新器、CFBundleVersion 为 0.1.6 的签名公证测试副本；它不是历史 0.1.6。旧版用户必须先手动安装 0.1.7 一次。

## 实际 HTTPS 更新结果

- 发布提交 `74671dd7c367261984549b784df7cd80908983ff`，CI `34464844566` 成功；GitHub 下载回读的 DMG、SHA256SUMS、appcast 三份文件与本机逐字节哈希一致。
- 从低 build 测试副本手动检查，显示正式 0.1.7；完成下载、解压、安装及重新打开。安装后 build 为 0.1.7，主程序 SHA256 与发布构建一致，嵌套签名验证通过。
- 真实右键菜单显示“当前版本：0.1.7”；再次检查返回“0.1.7 is currently the newest version available”。开机启动状态仍为 enabled，运行路径为 /Applications/NotchQuota.app。
- 发布前启动的测试进程曾得到 feed 404，发布后短时间内重试仍为 404；HTTP 独立请求已返回 200，重新打开测试应用后成功获取。未通过改写订阅或绕过签名来完成测试。
- DMG 已附公证票据。DMG 内应用未预附独立票据，更新后在线 staple 成功；默认安全策略下首次离线安装仍需独立机器验证。

本轮验证的是新功能与更新链路，没有重新运行动画压力跟踪，也没有恢复已关闭的持续性能监控。
