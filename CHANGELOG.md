# Changelog

## 0.1.15

- 手动添加实例明确区分应用与认证文件两步；认证选择默认打开 .gemini、显示隐藏文件，并显式允许无扩展名的 OAuth 文件。

## 0.1.14

- 修复侧栏收缩时黑色背景继续淡出造成的黑影；收起立即清除背景动画，保留透明图标过渡。

## 0.1.13

- 新增侧边栏模式，固定排列勾选账号，悬停查看详情、点击固定；与原有模式共用查询缓存。
- 支持自由拖动、左右贴边和多屏位置记忆，鼠标跨屏不再导致侧栏跳屏。
- 提供原生胶囊、磨砂圆球、迷你仪表、小幽灵四种收起外观，选择即时生效并保存。
- 小幽灵倾斜探头、贴边露出半个身体，没有小手；自由悬浮时完整显示。
- 收起图标保持静止，鼠标移入立即展开，离开 3 秒后自动收起；固定详情时保持展开。
- 修复详情切换时的标题裁切、空白、箭头错位、提示遮挡，以及淡出时重新打开的动画冲突。
- 保留减少动态效果与低电量降级；新增几何边界、交互状态和七账号性能验证场景。

## 0.1.12

- 新增菜单栏模式和单色额度图标，点击打开当前账号详情。
- 面板展示各周期、模型额度和真实重置时间；点击头像循环切换勾选账号，只有一个时保持不变。
- 详情宽度 320 点，高度随额度项数量调整；保留每分钟自动轮换和轻量切换过渡。
- 两种模式共用查询缓存和账号选择，保留临时隐藏恢复入口。
- 本机验证环境为 macOS 26.5.2；macOS 27 系统溢出区域仍需真机验收。

## 0.1.11

- 隐藏期间临时显示 Dock 图标，点击图标或重新打开应用即可恢复，避免菜单栏拥挤时找不到入口。

- 修复头像下载失败被缓存 24 小时的问题；一分钟后允许随正常刷新重试，更新失败保留已有头像。已确认身份时，额度服务失败不再阻止头像加载。

- 精简右键主菜单，账号管理和设置更新归入子菜单；菜单从面板下方展开，避免靠近刘海时被遮挡。

- 新增临时隐藏：15 分钟、1 小时、3 小时、5 小时，到期自动恢复。
- 隐藏期间可从菜单栏立即显示；暂停界面轮换，保留账号选择、后台额度刷新和跨重启截止时间。

## 0.1.10

- Antigravity 账号默认优先显示邮箱，缺少邮箱时回退到昵称；保留自定义别名。
- 邮箱仅用于内存中的显示，复用已有身份查询，不增加请求频率。

## 0.1.9

- 支持 Antigravity 独立实例发现、手动添加和逐账号显示控制，额外实例默认关闭。
- 多账号头像、编号回退和本机别名；账号变化需重新确认，凭据及额度缓存独立。
- 允许隐藏全部账号，通过临时菜单栏设置入口恢复。
- 增加真实多账号续期验证及防串号回归测试。

## [0.1.8] - 2026-09-10

- 右键菜单可选择显示的应用，只选择一个时固定显示；未选应用不查询额度。
- 增加自动轮换开关，选择在重启和更新后保留。

## [0.1.7] - 2026-09-10

- Sparkle 原生在线更新、每天检查、可更新标记和手动安装重启。
- 更新源和安装包 Ed25519 签名；支持在菜单关闭自动检查。

## [0.1.6] - 2026-09-09

- 每分钟轮换已安装应用，柔和淡入淡出；交互和休眠时暂停，自动切换只显示缓存，不增加额度请求。
- 收起详情保留当前应用；单应用不轮换。

## [0.1.5] - 2026-09-09

- 右键菜单增加“开机自动启动”，采用 macOS 原生 SMAppService 登录项。
- 状态直接读取系统，区分已开启、关闭和待允许；支持取消待允许的注册。
- 不增加后台辅助进程，不在启动时自动改回用户的选择。

## [0.1.4] - 2026-09-09

- 移除全桌面鼠标移动监听，减少无关事件处理。
- 布局动画不再重复设置图标、提示及辅助功能标签。
- 手动收起后避免悬停立即重新展开；展开/收起提示同步更新。
- 屏幕和系统休眠暂停新查询，唤醒恢复。
- 增加开发者使用的独立有界资源采样脚本，不加入应用运行依赖。

## [0.1.3] - 2026-09-09

- 移除刘海额度线及呼吸动画，改为紧凑图标与百分比常驻。
- 默认按 Codex → Claude → Antigravity 的安装优先级显示；15 秒无操作收起详情并回到默认应用。
- 空闲时停止交互定时器，后台查询只更新额度，保持每 3 分钟刷新。

## [0.1.2] - 2026-09-09

- 刘海额度线改为高饱和度颜色、2.1 点实线与轻微柔光，改善亮色壁纸上的对比度。
- 待机时约 4.8 秒一轮缓慢呼吸，仅使用系统合成器透明度动画。
- 展开、休眠、低电量及减少动态效果时停止呼吸；未知/旧数据静态显示。

## [0.1.1] - 2026-09-09

- 三枚应用图标去除不同透明边距，统一为 16 点视觉尺寸。
- 隐藏后显示沿刘海外缘的 1.5 点静态额度线，按 Codex → Claude → Antigravity 的安装优先级选择。
- 显示、隐藏、展开收起与应用切换增加约 0.2 秒的轻量过渡。
- 尊重系统减少动态效果与低电量模式；缓存图标，无常驻动画循环。
- 仅显示已安装的应用；单应用不切换，无应用时保持隐藏。
- 启动、唤出及每轮后台刷新重新检查安装状态，后台发现应用不会自动弹出。
- 增加安装组合、动画反向操作和空状态回归检查。

## [0.1.0] - 2026-09-09

首个公开版本。

- 原生 Apple Silicon 刘海额度视图与普通屏幕回退。
- 原生图标依次切换 Codex、Claude、Antigravity。
- 剩余额度绿/黄/红分级，未知/过期状态单独显示。
- 15 秒无操作自动隐藏，顶部热点唤出。
- 已保存登录的独立额度查询，原应用无需保持运行。
- 标准 Swift Package、回归测试、GitHub CI 和签名公证发布脚本。

### 已知边界

- 需要对应账号已有有效登录和可用网络；不支持第三方 API 中转余额。
- 额度接口与登录存储可能随原应用升级变化。
- 首版尚未覆盖每个 M 系列机型、macOS 版本和账户类型。
